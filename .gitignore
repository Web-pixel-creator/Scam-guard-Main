# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Local browser QA artifacts
.playwright-cli/
output/playwright/

# Local STT fixture captures may contain user voice evidence or sanitized
# provider transcripts that still need review before becoming regression data.
private/voice-stt-fixtures/
private/telegram-inline-qa/
ai_docs/voice-stt-provider-transcripts.local.json

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Lockfiles: this is a Bun project (bun.lock is the source of truth).
# package-lock.json is an npm artifact (e.g. from `npm install` during local
# testing) and must not be committed — two lockfiles drift and conflict.
package-lock.json

# Local env (never commit real secrets — see .env.example)
.env
.env.*
!.env.example

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.claude/
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
supabase/.temp/
