# File Map

Where things live. `src/routeTree.gen.ts` is auto-generated; never edit it by hand.

## Root

| Path                                                                         | Purpose                                                                                                                                                                                                                                                                                                   |
| ---------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `package.json`                                                               | Scripts plus pinned security overrides and guarded staging-only Supabase linked-command recipes.                                                                                                                                                                                                          |
| `vite.config.ts`                                                             | TanStack/Vite/Nitro config. Forces Nitro `node-server`, `src/server.ts` and an IPv4-loopback-only default dev listener; an external bind requires an explicit CLI `--host` override.                                                                                                                      |
| `vitest.setup.ts`                                                            | Test isolation boundary: mocks configured local providers and rejects every unmocked `fetch` before an external AI, reputation, Telegram or production request can leave the Vitest process.                                                                                                              |
| `Dockerfile`, `.dockerignore`                                                | Multi-stage production image for the Node SSR artifact; ships only the three QR worker decoder packages required by the isolated eval worker plus the bundled operational soak runners.                                                                                                                   |
| `.github/workflows/ci.yml`                                                   | Commit-SHA-pinned GitHub verification: install, lint, typecheck, full tests, production build, coverage floors and clean Supabase migration/schema/pgTAP checks.                                                                                                                                          |
| `.github/workflows/security.yml`                                             | Commit-SHA-pinned CodeQL, full-history Gitleaks, release-container Trivy High/Critical gate and CycloneDX SBOM artifact.                                                                                                                                                                                  |
| `railway.toml`                                                               | Railway config: Docker builder, `/healthz`, restart policy.                                                                                                                                                                                                                                               |
| `.env.example`                                                               | Documented runtime env vars. Real `.env` is ignored but must still have owner/admin-only filesystem ACLs; ignore status is not access control.                                                                                                                                                            |
| `bun.lock`, `bunfig.toml`                                                    | Canonical Docker/Bun dependency graph; security regressions require patched Vite/esbuild/Babel/js-yaml/brace-expansion resolutions and zero `bun audit` findings. npm also works for scripts.                                                                                                             |
| `supabase/config.toml`                                                       | Supabase project config.                                                                                                                                                                                                                                                                                  |
| `ai_docs/RECOVERY_AND_KEY_ROTATION.md`                                       | Production-safe backup/restore, Railway rollback, key-rotation and Supabase Auth hardening runbook with explicit evidence boundaries.                                                                                                                                                                     |
| `ai_docs/HOSTED_STAGING_RESTORE_DRILL_2026-07-28.md`                         | Functional hosted restore/service evidence, guarded hardening-history closeout, same-client AAL1/AAL2 PostgREST proof and staging cleanup boundary; complete RPO/RTO timing remains open.                                                                                                                 |
| `ai_docs/PRODUCTION_MIGRATION_PREFLIGHT_2026-07-29.md`                       | Historical read-only production baseline, migration manifest and compensation analysis captured before the completed apply.                                                                                                                                                                               |
| `ai_docs/PRODUCTION_MAINTENANCE_WINDOW_2026-08-02.md`                        | Completed local-date production database window record, exact-image resume summary and future freeze safety boundary.                                                                                                                                                                                     |
| `ai_docs/PRODUCTION_MIGRATION_APPLY_2026-08-01.md`                           | Sanitized exact Supabase apply, postflight, Railway resume and observation evidence.                                                                                                                                                                                                                      |
| `ai_docs/CANARY_72H.md`                                                      | Fixed-RC 72-hour canary entry, monitor, restart, billing and privacy-safe closure evidence contract.                                                                                                                                                                                                      |
| `ai_docs/POLLING_RESOURCE_SOAK_2026-07-14.md`                                | Sanitized exact-build evidence for the successful 60-minute Railway polling/resource run, including queue, memory, event-loop, update-latency and failure-model results plus the remaining physical-restart boundary.                                                                                     |
| `ai_docs/QR_WORKER_RESOURCE_SOAK_2026-07-14.md`                              | Sanitized exact-build evidence for the production QR runtime packaging correction and successful ten-minute PNG/JPEG corpus, queue, CPU/RSS/event-loop and worker termination/recovery profile.                                                                                                           |
| `ai_docs/SHARED_RATE_LIMIT_FAILURE_SMOKE_2026-07-14.md`                      | Exact-build evidence for the isolated six-case production shared-limiter failure probe, real-consumer 429 ordering, post-probe health and count-only bucket observation.                                                                                                                                  |
| `ai_docs/TELEGRAM_RESTART_QA_2026-07-14.md`                                  | Privacy-safe closure evidence for the real Railway instance replacement, polling-leader re-election and one approved Telegram update with no observed duplicate reply.                                                                                                                                    |
| `supabase/migrations/*.sql`                                                  | DB schema history, including service-role-only aggregate stats/session RPCs, confirmed-only public report/loss impact counters, privacy-safe embed origin telemetry and exact admin-role eligibility reconciliation.                                                                                      |
| `supabase/migrations/20260729105030_family_notification_claim_retention.sql` | Production-applied forward migration, previously pgTAP-verified in isolated staging, adding expired private Family notification claims to scheduled retention without changing the cron schedule.                                                                                                         |
| `supabase/migrations/20260729131000_admin_mfa_aal2_rls.sql`                  | Production-applied forward migration, previously pgTAP-verified in isolated staging, requiring role-plus-AAL2 for protected direct authenticated admin RLS reads/updates while preserving public confirmed rows and service-role bypass.                                                                  |
| `supabase/migrations/20260711010000_telegram_session_update_sequence.sql`    | Adds `last_update_id`/`last_update_at` and a service-role-only atomic session patch RPC; older writes are stale except after the seven-day inactivity epoch required for Telegram's documented id reset.                                                                                                  |
| `scripts/prod-smoke.ts`                                                      | Delivery-mode-aware one-shot production smoke for the public app, health, authenticated webhook boundary, Telegram update state, polling leader and AI provider; live webhook injection is refused in polling mode in favor of the polling dispatch harness.                                              |
| `scripts/prod-monitor.ts`                                                    | Recurring production monitor for public app, `/healthz`, Telegram webhook state and AI provider; can send sanitized Telegram alerts to an operator chat.                                                                                                                                                  |
| `scripts/prod-monitor-policy.ts`                                             | Pure severity/exit policy for recurring monitoring. Required missing Telegram secrets are failures; optional local skips remain warnings.                                                                                                                                                                 |
| `scripts/admin-role-preflight.ts`                                            | Count-only production preflight for exact admin entitlement drift. It emits no user id or email and exits non-zero when stale or missing admin roles exist.                                                                                                                                               |
| `scripts/supabase-linked-command.ts`                                         | Guarded Supabase CLI wrapper: only status, linked migration list and dry-run remain live; they hard-block production, require linked/env/manual staging refs to agree and pass a minimized child environment. Mutating repair/push recipes were retired after closeout.                                   |
| `scripts/hosted-staging-mfa-smoke.ts`                                        | Synthetic staging TOTP smoke; after the local AAL2 migration it probes one protected fixture with the same user client at AAL1 and AAL2, then deletes and reads back every synthetic object.                                                                                                              |
| `scripts/polling-resource-soak.ts`                                           | CLI for the non-network polling/resource soak. The Docker build bundles it into `dist/ops` so Railway can measure the real runtime without source files, package managers, API quota or persisted synthetic records.                                                                                      |
| `scripts/shared-rate-limit-failure-smoke.ts`                                 | Short-lived production-image probe for missing config, hash failure, RPC error/shape/transport failure and real-consumer fail-closed ordering. It replaces fetch inside the process, exposes no app endpoint and performs no external call or write.                                                      |
| `.github/workflows/prod-monitor.yml`                                         | Scheduled production monitor. Secret-backed checks are required, but credentials exist only in the final monitor step; checkout/setup actions are commit-SHA pinned, checkout credentials are not persisted and Bun is version-pinned.                                                                    |
| `scripts/security-smoke-env.ts`                                              | Pure env guards for production security smoke, including proxy IP header trust proof.                                                                                                                                                                                                                     |
| `scripts/moderation-alert-smoke.ts`                                          | Safe smoke test for the optional private moderation chat; sends a clearly marked non-user alert without user evidence or secrets, and can explicitly verify high-signal research alerts with `-- --research`.                                                                                             |
| `scripts/prod-telegram-inline-smoke.ts`                                      | Delivery-mode-aware synthetic Inline transport/non-persistence smoke. Polling requires webhook `503` and zero side effects; only a genuine Telegram client can prove handler rendering/insertion.                                                                                                         |
| `scripts/telegram-inline-client-matrix.ts`                                   | Offline generator for the exact 17-case Telegram Inline client pack: 51 Desktop/Android/iOS rows, RU/UZ/EN risk levels, 0/1/255/256 boundaries and synthetic privacy fixtures; performs no network call.                                                                                                  |
| `scripts/prod-family-shield-smoke.ts`                                        | One-shot production smoke test for Family Shield invite/accept/notify/revoke storage paths using synthetic Telegram ids and no secret output.                                                                                                                                                             |
| `ai_docs/ON_CALL_RUNBOOK.md`                                                 | Production monitor alert triage, recovery commands and escalation boundaries.                                                                                                                                                                                                                             |
| `.kiro/specs/telegram-bot-mvp/`                                              | Kiro requirements/design/tasks for the Telegram bot MVP.                                                                                                                                                                                                                                                  |
| `.kiro/specs/*/`                                                             | Feature specs for Telegram UX, brand impersonation, meta intent, image/link intelligence, inline check, menu/result polish, emergency copilot, response compression, phone directory, webhook dedup, shared rate limits, voice STT/TTS, research-feed, website trust/trends/impact and embed-widget work. |
| `.lovable/project.json`                                                      | Historical template marker only; not a production runtime target.                                                                                                                                                                                                                                         |

## `src/` entry points

| Path                  | Purpose                                                                                                                                                                                      |
| --------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `server.ts`           | Node/Nitro server entry. Adds `/healthz`, binds Telegram webhook, applies CSP/security headers and the dynamic HTTP compression wrapper, then delegates to SSR.                              |
| `start.ts`            | TanStack Start setup: request error middleware and server-function middleware.                                                                                                               |
| `router.tsx`          | TanStack Router + Query client.                                                                                                                                                              |
| `routeTree.gen.ts`    | Auto-generated route tree.                                                                                                                                                                   |
| `styles.css`          | Tailwind v4, brand tokens, accessibility modes and imports for the approved production/editorial surfaces.                                                                                   |
| `approved-design.css` | Compatibility styling for the existing translated production routes and shared chrome.                                                                                                       |
| `signal-exact.css`    | Approved homepage/admin visual system: exact production stripe, editorial grids, responsive rules, reduced-motion CTAs, task-first admin controls and smooth disclosure/confirmation states. |

## `src/routes/`

| File                   | URL                 | Notes                                                                                                                                                                              |
| ---------------------- | ------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `__root.tsx`           | app shell           | Providers, responsive Header/Footer, SEO meta, language sync and route-specific chrome boundaries for the homepage, embed and admin.                                               |
| `index.tsx`            | `/`                 | Chooses the exact approved Russian homepage while preserving the established translated UZ/EN production landing experience.                                                       |
| `check.tsx`            | `/check`            | Dedicated check page.                                                                                                                                                              |
| `embed.tsx`            | `/embed`            | Partner-facing iframe snippet generator and live preview.                                                                                                                          |
| `embed.check.tsx`      | `/embed/check`      | No-chrome iframe runtime for compact partner-site checks, including compact Risk Passport summaries and privacy-safe origin usage telemetry for shallow phone/Telegram checks.     |
| `report.tsx`           | `/report`           | Scam report form.                                                                                                                                                                  |
| `emergency.tsx`        | `/emergency`        | First-hours recovery checklist.                                                                                                                                                    |
| `official-numbers.tsx` | `/official-numbers` | Public verified-contact directory for safe callback numbers and official Telegram/email contacts.                                                                                  |
| `scam-trends.tsx`      | `/scam-trends`      | Public non-personal map/index of current scam tactics, privacy-safe category buckets, hooks, goals, safe steps and reason-code coverage.                                           |
| `privacy.tsx`          | `/privacy`          | Privacy notice.                                                                                                                                                                    |
| `appeal.tsx`           | `/appeal`           | Privacy-safe reputation correction/removal request page for phone, Telegram, URL and APK targets.                                                                                  |
| `login.tsx`            | `/login`            | Supabase email auth with invitation/list-of-access language instead of internal allowlist jargon.                                                                                  |
| `admin-mfa.tsx`        | `/admin-mfa`        | TOTP enrollment/challenge route used to refresh an eligible admin session to AAL2; never exposes or stores QR/TOTP material outside the Supabase Auth flow.                        |
| `admin.tsx`            | `/admin`            | Role-plus-AAL2 moderation dashboard with compact session status, queue search/test filtering, deliberate decision confirmations and preserved report, appeal and entity mutations. |

## Approved public UI components

| File                                         | Purpose                                                                                                                                                                                        |
| -------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/components/ApprovedRussianHomepage.jsx` | Approved Russian homepage composition with one clear prevention flow, above-fold emergency recovery, live check/OCR/report paths, factual aggregates and established education/privacy routes. |
| `src/components/ApprovedHomeHero.tsx`        | Approved three-line homepage hero wired to the real `CheckInput`, OCR/result state, report flow and Telegram bot link.                                                                         |
| `src/components/Layout.tsx`                  | Shared responsive site header and footer using the approved typography, spacing, navigation and floating-points CTA treatment.                                                                 |
| `src/components/CheckInput.tsx`              | Shared production checker; the `signal` variant uses one auto-detecting input, explicit demo labelling and real screenshot OCR without forking server-side risk evaluation.                    |
| `src/components/QuickReportForm.tsx`         | Shared report flow; the `signal` variant renders the exact community panel while retaining real submission and validation.                                                                     |
| `src/components/StatsStrip.tsx`              | Live aggregate proof strip with approved section styling hook.                                                                                                                                 |
| `src/components/HomeImpactCounters.tsx`      | Privacy-safe aggregate impact counters with approved section styling hook.                                                                                                                     |
| `src/components/HomeTrustSurface.tsx`        | Verified official-contact directory surface with approved section styling hook.                                                                                                                |
| `src/components/HomeSchemeTrends.tsx`        | Static/aggregate scheme-trend surface with approved section styling hook.                                                                                                                      |

## `src/lib/`

| Path                                                       | Purpose                                                                                                                                                                                                                                                                                                                                 |
| ---------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `check.functions.ts`                                       | Web server functions wrapping check/OCR core plus cached service-role-backed public stats; report/loss fallback aggregates are confirmed-only.                                                                                                                                                                                          |
| `request-ip.server.ts`                                     | Shared public rate-limit key helper; ignores proxy IP headers unless `TRUST_PROXY_IP_HEADERS=true` and falls back to the direct request IP.                                                                                                                                                                                             |
| `embed-origin-analytics.server.ts`                         | Service-role-only `/embed/check` usage telemetry helper; normalizes partner/referrer origin metadata and aggregate result shape without storing raw input, full URLs, paths, query strings, phone numbers or Telegram ids.                                                                                                              |
| `embed-widget.ts`                                          | Partner iframe widget helpers: language fallback, partner label sanitization and safe snippet generation.                                                                                                                                                                                                                               |
| `security/csp.ts`                                          | Main-site and embed Content Security Policy builders, including nonce insertion and explicit `/embed/check` frame-ancestor allowlist parsing.                                                                                                                                                                                           |
| `http-compression.server.ts`                               | Dynamic GET Brotli/gzip negotiation. Local hardening uses an abort-aware pipeline, strict quality parsing and `406` for a complete encoding prohibition; Nitro's earlier static handler still has an open general `q`-weight limitation.                                                                                                |
| `report.functions.ts`                                      | Public report server function; prepares target hashes/masked displays, redacts descriptions before insert, stores same-day duplicates as private `duplicate` evidence, refreshes private entity candidates without bumping public counts, and keeps situation-only reports out of entity reputation.                                    |
| `quick-report-payload.ts`                                  | Homepage quick-report payload helper; empty optional targets become incident-only reports instead of placeholder entity reports.                                                                                                                                                                                                        |
| `reputation-appeal.functions.ts`                           | Public reputation appeal server function; accepts only concrete targets, hashes/masks identifiers, redacts reasons and deduplicates active review requests.                                                                                                                                                                             |
| `report-boundary.ts`                                       | Shared incident-only report marker/helpers used by report submission and admin moderation.                                                                                                                                                                                                                                              |
| `admin.functions.ts`                                       | Role-plus-AAL2 admin moderation server functions; lists reports/entities, syncs confirmed reputation, skips entity sync for situation-only reports and reviews appeals with redacted audit logging.                                                                                                                                     |
| `admin-mfa.server.ts`, `admin-mfa.server.test.ts`          | Server-function AAL2 assertion plus local/production configuration contract. Production/Railway requires an explicit flag; missing/invalid configuration fails closed.                                                                                                                                                                  |
| `security/admin-mfa-rls-migration.test.ts`                 | Static contract for the production-applied role-plus-AAL2 RLS migration without weakening public confirmed-row policies or service-role behavior.                                                                                                                                                                                       |
| `security/hosted-staging-mfa-smoke-contract.test.ts`       | Prevents the hosted MFA smoke from substituting a local assertion or service-role read for same-user-client AAL1/AAL2 evidence.                                                                                                                                                                                                         |
| `security/supabase-linked-command.test.ts`                 | Fail-closed contract for production-link refusal, exact staging ref confirmation and shell-free fixed Supabase CLI recipes.                                                                                                                                                                                                             |
| `admin-allowlist-migration.test.ts`                        | Static regression tests for the Supabase admin allowlist migration: no admin grant before email confirmation, confirmation trigger present, and unverified auto-grants cleaned up.                                                                                                                                                      |
| `admin-role-lifecycle-migration.test.ts`                   | Static regression for exact admin entitlement projection, allowlist/Auth negative-transition triggers, internal function privilege revocation and migration backfill.                                                                                                                                                                   |
| `admin-role-preflight.ts`                                  | Pure count-only comparison of confirmed allowlist eligibility and durable admin-role rows; intentionally returns no identifiers.                                                                                                                                                                                                        |
| `public-impact-counters-migration.test.ts`                 | Static regression tests for the public impact counter migration: report/loss aggregates stay confirmed-only and service-role-only.                                                                                                                                                                                                      |
| `retention-audit-migration.test.ts`                        | Static regression tests for Supabase retention cleanup, scheduled cron usage, admin audit RLS and audit/appeal preservation.                                                                                                                                                                                                            |
| `telegram/family-notification-retention-migration.test.ts` | Static contract that migration `20260729105030` prunes expired private Family claims without deleting active links or weakening retention-function privileges.                                                                                                                                                                          |
| `risk/check-core.ts`                                       | Transport-independent rules-first check, OCR and image-analysis pipelines; verified metadata/Safe requires an exact standalone contact, composite destinations remain available to deterministic URL/brand scoring, and persisted input is centrally redacted.                                                                          |
| `risk/rules.ts`                                            | Deterministic ReasonCode rules, weights, labels and exhaustive `REASON_TRUST_IMPACT` classification; verified-official protection fails closed when any risk-classified reason is present.                                                                                                                                              |
| `risk/mixed-clause-adversarial-corpus.ts`                  | Offline 363-message RU/UZ/EN polarity/separator corpus: 330 neutral/safety-plus-danger combinations and 33 safe controls across 11 sensitive action families. It is generated QA data, not training or live user evidence.                                                                                                              |
| `risk/domain-normalizer.ts`                                | Produces a lossless WHATWG/IDNA DNS identity for trust/allowlists separately from the NFKC visual-confusable/Cyrillic similarity skeleton used only for additive detection.                                                                                                                                                             |
| `risk/brand-matcher.ts`                                    | Protected-brand URL/text matcher; official/news suppression uses lossless DNS identity, while alias/segment similarity and Unicode-aware text boundaries remain detection-only.                                                                                                                                                         |
| `risk/risk-passport.ts`                                    | Shared pure presenter for compact low-signal phone/Telegram Risk Passport sections used by website, embed and Inline UI; model explanations cannot select a passport or become evidence, and optional Telegram sections require separately typed deterministic provenance/text.                                                         |
| `risk/ai-output-safety.ts`                                 | Clause-local safety firewall for AI-authored user-facing explanations; blocks prompt-injection leakage and requests for secrets, APKs, wallet actions or payments without letting one negated warning exempt a sibling command, and opens a short per-key cooldown after repeated unsafe outputs.                                       |
| `risk/image-intelligence.ts`                               | Structured Telegram image evidence: complete pre-redaction OCR URL tokens and pixel-decoded destinations remain in deterministic scoring, while provider-only guesses/prefix substitutions do not; QR Wi-Fi/password/OTP/recovery/authenticator/private-invite secrets are removed before check input and protective risk hints remain. |
| `risk/qr-decoder.ts`                                       | Fail-closed PNG/JPEG QR worker boundary: 4 MiB/4 MP input, bounded downscaled scan work, one isolated worker, four-job backlog, per-job deadline and worker memory limits; an internal operations-only termination hook supports crash/recovery evidence.                                                                               |
| `risk/qr-worker-resource-soak.ts`                          | Isolated in-memory PNG/JPEG/non-QR/malformed/oversize corpus and resource profile with queue-bound, forced worker termination and post-recreation decode checks; no external calls or persistent writes.                                                                                                                                |
| `risk/media-data-url.ts`                                   | Shared allowlist validator for AI-bound image data URLs: png/jpeg/webp only, base64 only, decoded screenshot byte limit enforced.                                                                                                                                                                                                       |
| `risk/detect.ts`                                           | Input detection, normalization and the central privacy boundary for display masking/redaction; it first applies the shared credential sanitizer, then the existing phone/card/URL/custom-scheme controls.                                                                                                                               |
| `risk/uz-cyrillic-translit.ts`                             | Classifier-only Uzbek Cyrillic→Latin transliteration; `evaluateText` unions reason codes from the original text and this matching variant so Cyrillic-script Uzbek reaches the Latin rule patterns. Never replaces user text in replies, persistence or logs.                                                                           |
| `telegram/__qa__/`                                         | Elderly-realism observational QA corpus and harnesses: 100 realistic cases with RU/UZ coverage plus a small EN slice through the real dispatch/inline pipeline with faked Telegram/Supabase. It now locks the three 2026-07-29 semantic tails but is not a balanced full semantic oracle or live-client evidence.                       |
| `risk/sensitive-text.ts`                                   | Idempotent RU/UZ/EN sink sanitizer for labeled passwords/passphrases, separated OTP/PIN/CVV, labeled recovery phrases and private keys, with legitimate prose/amount/phone compatibility guards.                                                                                                                                        |
| `risk/phone-intelligence.ts`                               | Honest phone-number passport: country/calling code, Uzbekistan prefix/operator hint, official-directory status and verified-contact lookalike hints without owner inference.                                                                                                                                                            |
| `risk/phone-reputation.ts`                                 | Confirmed, moderated phone reputation summary and shared RU/UZ/EN source/scope wording built from `entities`; no owner/carrier/hidden-label claims.                                                                                                                                                                                     |
| `risk/url-reputation.server.ts`                            | Optional external URL reputation checks for Google Safe Browsing, URLhaus and PhishTank with origin-only provider payloads, short cache and in-flight de-duplication; userinfo/path/query/fragment remain local.                                                                                                                        |
| `risk/hash.ts`                                             | Identifier hashing.                                                                                                                                                                                                                                                                                                                     |
| `risk/rate-limit.ts`                                       | Bounded local/test sliding-window fallback: validated parameters, 4096-key TTL/LRU cap, fail-closed overflow and rate-limited full expiry cleanup.                                                                                                                                                                                      |
| `risk/shared-rate-limit.server.ts`                         | Service-role Supabase `claim_rate_limit()` boundary with HMAC-only persisted keys; production/Railway fails closed on missing config, hashing/RPC errors or invalid responses, while only non-production may use the bounded local fallback.                                                                                            |
| `risk/verified-contacts.ts`                                | Official Uzbekistan contact seed and active lookup. Mutable Telegram handles expire after 30 days without renewed primary-source verification and are excluded from verified matching/counts.                                                                                                                                           |
| `trust/official-directory.ts`                              | Active-contact public-directory helpers: stats, search/filter, contact actions and source-link detection; expired Telegram handles cannot produce public links.                                                                                                                                                                         |
| `trust/scheme-trends.ts`                                   | Static public scheme-trend helpers: non-personal tactic entries, stats, category/search filters and severity ordering.                                                                                                                                                                                                                  |
| `trust/scam-map-index.ts`                                  | Privacy-safe public scam map/index helper: category buckets, national layer, regional suppression thresholds and forbidden-field guards for future dynamic data.                                                                                                                                                                        |
| `trust/impact-stats.ts`                                    | Public-stats normalizer and formatters for aggregate-only website impact counters. Check/risk values are raw activity; report/loss values are confirmed impact.                                                                                                                                                                         |
| `meta-intent.ts`                                           | Deterministic RU/UZ/EN classifier for questions to the bot itself. Capability grammar distinguishes actual requests from past-action/emergency narration and refuses misleading phone/Telegram routes for unsupported card/order/account objects.                                                                                       |
| `telegram/emergency.ts`                                    | Dynamic emergency checklist builder, panic menus and Emergency Copilot follow-up routing.                                                                                                                                                                                                                                               |
| `telegram/router.ts`                                       | Telegram update router (dispatch, parseCommand, decideRoute), including direct `/call`; passes the Inline sender language hint to first-contact session loading and resets active/contextual session state when its stored chat scope does not match the current update.                                                                |
| `telegram/handlers/index.ts`                               | Handler aggregator — wires commands/check/report/misc into the router.                                                                                                                                                                                                                                                                  |
| `telegram/handlers/commands.ts`                            | /start, /call, /lang, /help, /safety, /emergency, /panic, /check, /report.                                                                                                                                                                                                                                                              |
| `telegram/handlers/check.ts`                               | Text/image/contact handlers; per-turn RU/UZ/EN, enum-only recent-victim and emergency context, explicit bank-number extraction, secret-safe guidance and early media admission. Local fixes preserve UZ bank/code confirmation and its following emergency next step.                                                                   |
| `telegram/media-admission.server.ts`                       | Shared `telegram-image:tg:<userId>` admission helper used by ordinary image and `/report` screenshot paths before `getFile` or download.                                                                                                                                                                                                |
| `telegram/check-followup.ts`                               | Typed post-check/orphan RU/UZ/EN actions; shares ranked evidence truth and concrete-artifact routing with Inline, arbitrates recent panic/check context by timestamp and selects high-risk steps from reason-bound protective actions.                                                                                                  |
| `telegram/intent-contract.ts`                              | Canonical namespaced meta/victim/follow-up/panic/risk-input registry with response actions and direct/Inline persistence plus trusted-contact boundaries.                                                                                                                                                                               |
| `telegram/victim-intent.ts`                                | RU/UZ/EN human-situation classifier and action-first copy. Local priorities keep bank/card code theft above broad Telegram takeover, retain UZ contextual confirmation and recognize the common Russian `безапасный счет` typo as a no-transfer safe-account scheme.                                                                    |
| `telegram/intent-text-normalization.ts`                    | Classifier-only NFKC/invisible-format/confusable normalization; never replaces original display, persistence or secret-sanitization input.                                                                                                                                                                                              |
| `telegram/inline-query-language.ts`                        | Shared per-message RU/UZ/EN resolver for Direct and stateless Inline, including narrow Uzbek Cyrillic/transliteration evidence.                                                                                                                                                                                                         |
| `telegram/reply-check-context.ts`                          | Bounded reply-to-bot message-id context for recent check/guardian follow-ups with sanitized coarse metadata only.                                                                                                                                                                                                                       |
| `telegram/sensitive-secret-input.ts`                       | Detects supplied OTP/PIN/CVV/password/recovery material and builds non-echoing localized containment guidance.                                                                                                                                                                                                                          |
| `telegram/adversarial-human-scenario-corpus.ts`            | Offline 1,080-case RU/UZ/EN human-scenario matrix across typo, Unicode/confusable, multiline and concrete-artifact mutations for Direct/Inline semantic oracles.                                                                                                                                                                        |
| `telegram/inline-context-robustness-corpus.ts`             | Offline 1,175-case Inline multiline/follow-up robustness matrix with safe, privacy and boundary controls.                                                                                                                                                                                                                               |
| `telegram/dialogue-corpus.ts`                              | Generated 1,872-row RU/UZ/EN post-check dialogue DSL across recent, orphan, stale and new-artifact contexts, including reviewed reply-to-bot and exact common-typo variants.                                                                                                                                                            |
| `telegram/conversation-wrapper.ts`                         | Small allowlisted RU/UZ/EN normalizer for harmless conversational lead-ins; preserves wrapper-only acknowledgements and never replaces concrete-artifact detection on the original message.                                                                                                                                             |
| `telegram/text-panic-intent.ts`                            | Pure shared RU/UZ/EN text/voice emergency classifier used by the real handler and offline dialogue QA; preserves negation, quoted/third-party, active-call, already-done and transliterated-input gates without owning persistence or Bot API effects.                                                                                  |
| `telegram/human-dialogue-corpus.ts`                        | Curated deterministic capability/meta/ordinary phrase surface: 336 reviewed core variants expanded across three explicit contexts into 1,008 offline cases. It is QA data, not model training or a claim of live Telegram conversations.                                                                                                |
| `telegram/synthetic-multiturn-dialogue-corpus.ts`          | Exactly 1,000 deterministic two-/three-turn RU/UZ/EN offline sequences spanning scam-risk, post-result and ordinary/meta conversation. The corpus builds real rule snapshots but does not call external APIs or replace real-client acceptance testing.                                                                                 |
| `telegram/inline-adapted-dialogue-corpus.ts`               | Stateless Inline adaptation of all 1,000 synthetic dialogues plus contextual follow-ups, mixed clauses and synthetic credential boundaries: 3,805 source cases / 2,140 unique RU/UZ/EN queries. The companion real-handler test denies network and writes; it is not live-client evidence.                                              |
| `telegram/everyday-dialogue-corpus.ts`                     | 540 semantically distinct two-turn RU/UZ/EN everyday QA dialogues across 15 scam, help, capability, follow-up and mixed-clause categories. Both answers come from production response builders, including the shared panic classifier/cards; the corpus is offline test data, not model training or live-chat evidence.                 |
| `telegram/concrete-artifact.ts`                            | Shared structural detector for URLs, bare/IDN domains, Telegram identifiers, real code/card/phone values and dangerous files; safety wording without an actual value is not a new artifact.                                                                                                                                             |
| `telegram/handlers/inline.ts`                              | Compact rules-only non-persistent Inline articles; full multiline routing, per-query RU/UZ/EN, topic-preserving/action-first preview and insertion, secret re-masking, 256-code-point and 120/4096 output bounds, safe meta gates, validated bot username and plaintext entity retry.                                                   |
| `telegram/inline-reason-presentation.ts`                   | Exhaustive 55-code priority/method/source/limitation policy plus the canonical result reason collector used by Inline and post-check, including official-directory and moderated-report metadata.                                                                                                                                       |
| `telegram/handlers/report.ts`                              | Multi-step /report scenario; claims media admission before file metadata/download, stores prepared target hashes/masked displays plus redacted draft text, and keeps screenshot bytes transient.                                                                                                                                        |
| `telegram/handlers/misc.ts`                                | Callbacks (language, report, check_another, emergency, panic) + out-of-scope.                                                                                                                                                                                                                                                           |
| `telegram/webhook.server.ts`                               | Compatibility webhook handler; token-first and fail-closed, returns 200 only after durable completion and keeps uncertain work retryable with 503.                                                                                                                                                                                      |
| `telegram/update-lifecycle.server.ts`                      | Strict service-role RPC adapter for singleton leader leases, update processing/completion leases and fence checks.                                                                                                                                                                                                                      |
| `telegram/updates-poller.server.ts`                        | Single-leader `getUpdates` supervisor with a default batch of 20, ordered stateful handling, bounded strict-Inline concurrency and a contiguous acknowledged frontier. Delivery remains at-least-once, not exactly-once.                                                                                                                |
| `telegram/outbound-effect-guard.ts`                        | Lightweight shared Bot API effect-guard registry without Node-only imports.                                                                                                                                                                                                                                                             |
| `telegram/outbound-effect-fence.server.ts`                 | Server-only DB lease/fence enforcement installed for outbound Bot API effects.                                                                                                                                                                                                                                                          |
| `telegram/update-execution.server.ts`                      | Async request-local Telegram `update_id`, loaded language and session-storage-failure state used by sequenced saves and one post-dispatch user warning.                                                                                                                                                                                 |
| `telegram/update-serialization.server.ts`                  | Per-user in-process webhook serialization; independent users stay concurrent and same-user ownership is retained until active work actually settles.                                                                                                                                                                                    |
| `telegram/update-dispatch.server.ts`                       | Combines per-user serialization with request-local execution state and invokes the privacy-safe session-failure notifier once after dispatch.                                                                                                                                                                                           |
| `telegram/api.server.ts`                                   | Telegram Bot API helpers (sendMessage, sendAudio, getFile, escapeMarkdownV2, etc.); `answerInlineQuery` returns a typed failure with optional Bot API code/description so callers do not mistake `{ok:false}` for delivery.                                                                                                             |
| `telegram/webhook-delivery-policy.ts`                      | Shared `max_connections=1` containment policy for webhook mode. It does not replace the fenced polling lifecycle and is not an exactly-once guarantee.                                                                                                                                                                                  |
| `security/telegram-delivery-policy.ts`                     | Pure webhook/polling policy shared by production smoke scripts: strict mode parsing, expected authenticated webhook status and update-delivery health invariants.                                                                                                                                                                       |
| `telegram/public-metadata.server.ts`                       | Best-effort Telegram username/link enrichment: target extraction, public `getChat`, private-link limitations, visible signals and next steps.                                                                                                                                                                                           |
| `telegram/public-post.server.ts`                           | Best-effort public Telegram post web fetch/parser; visible body, preview and inline-button text pass the sink credential sanitizer before rules input or persistence.                                                                                                                                                                   |
| `telegram/reputation.server.ts`                            | Privacy-safe Telegram reputation helpers: hashed target observation, moderated report sync, source/confidence user-facing brief.                                                                                                                                                                                                        |
| `telegram/family-shield.server.ts`                         | Family Shield trusted-contact helpers: hashed invites, active-link guard, invite TTL, metadata-only idempotency claims, redacted alerts, cooldowns and opt-out. Production migration `20260729105030` guarantees scheduled expiry pruning.                                                                                              |
| `telegram/voice-out.server.ts`                             | Opt-in SOS/Guardian Voice-out: short safe TTS scripts, sanitization, per-user budget, provider isolation and text fallback.                                                                                                                                                                                                             |
| `telegram/digest.ts`                                       | Weekly Telegram scam digest model and formatter: manual source/status/updated-at entries, stale/partial fallback, MarkdownV2 output and compact next-action keyboard.                                                                                                                                                                   |
| `telegram/moderation-notifier.server.ts`                   | Optional private moderation Telegram alerts for new reports, appeals and high-signal research items; sends redacted summaries, public scheme metadata and admin links only.                                                                                                                                                             |
| `telegram/forward-context.ts`                              | Reply-only context for public forwarded Telegram channel/group sources; sanitized title/username, no scoring or persistence impact.                                                                                                                                                                                                     |
| `telegram/image-fallback.ts`                               | Unreadable-image triage callbacks/keyboards for gift, casino, wallet, bank/code and menu/QR scenarios.                                                                                                                                                                                                                                  |
| `telegram/session.server.ts`                               | Session store (telegram_sessions via supabaseAdmin): fenced/monotonic writes, chat scope, prepared report drafts, bounded last-check provenance and enum-only `lastVictimIntent` with no raw evidence/amount/artifact.                                                                                                                  |
| `telegram/reputation.server.ts`                            | HMAC-keyed moderated Telegram reputation layer; aggregate moderation sync validates both exact counts and upsert, propagates typed stage-only partial failures and never treats DB failure as zero/success.                                                                                                                             |
| `telegram/bot-i18n.ts`                                     | Bot-specific trilingual strings (ru/uz/en).                                                                                                                                                                                                                                                                                             |
| `telegram/format.ts`                                       | Telegram response formatter (MarkdownV2, keyboards, verified badge).                                                                                                                                                                                                                                                                    |
| `telegram/advice-filter.ts`                                | Exhaustive typed `ReasonCode → ProtectiveActionId/null` policy plus prioritized RU/UZ/EN protective copy; high-risk results cannot fall through to a request for more context.                                                                                                                                                          |
| `config.server.ts`                                         | Server-only env helpers read per request. On the local branch, production/Railway must explicitly set `REQUIRE_ADMIN_MFA_AAL2`; missing/invalid values fail closed while dev/test may omit it.                                                                                                                                          |
| `vitest.network-guard.test.ts`                             | Regression proving the global Vitest fetch guard blocks any unmocked external request, so large offline dialogue suites cannot consume production AI/reputation/Telegram quota.                                                                                                                                                         |
| `error-capture.ts`, `error-page.ts`                        | Error capture/rendering.                                                                                                                                                                                                                                                                                                                |

## `src/components/`

`CheckInput`, `EmbedCheckWidget`, `RiskResultCard`, `QuickReportForm`, `StatsStrip`, `HomeImpactCounters`, `HomeTrustSurface`, `HomeSchemeTrends`, `OfficialContactsDirectory`, `ScamMapIndexPanel`, `SchemeTrendsPanel`, `Layout`, `LanguageSwitcher`, `A11yPanel`, `FancyButton`, `UnicornBackground`; `components/ui/*` are shadcn primitives.

## `src/integrations/supabase/`

Browser client, service-role server client, auth middleware/attacher and generated DB types.

## `scripts/`

Production smoke scripts:

- `prod-smoke.ts`: public app, `/healthz`, delivery-mode-aware webhook auth,
  Telegram pending state, polling leader and AI provider.
- `prod-telegram-inline-smoke.ts`: synthetic Inline transport/non-persistence
  boundary. In polling mode it proves webhook shutdown and zero side effects,
  but deliberately does not claim real Inline handler or client delivery.
- `telegram-inline-client-matrix.ts`: no-network generator for the bounded
  17-case-per-client Desktop/Android/iOS acceptance pack.
- `prod-monitor.ts`: recurring delivery-mode-aware app/Telegram/AI monitor with authenticated polling-leader health.
- `switch-telegram-to-polling.ts`: verifies an active polling leader before removing the webhook without dropping pending updates.
- `polling-resource-soak.ts`: controlled in-memory polling failure model and
  resource profile; emits progress/final JSON and makes no Telegram, database,
  reputation or AI-provider call.
- `qr-worker-resource-soak.ts`: parameterized isolated QR decoder corpus,
  queue/resource and worker termination/recovery profile; the Docker image
  bundles it at `dist/ops/qr-worker-resource-soak.mjs`.
- `prod-family-shield-smoke.ts`: synthetic Family Shield invite/accept/notify/revoke/cleanup.
- `prod-security-smoke.ts`: RLS/security checks for sensitive tables, reputation appeals, shared rate-limit buckets, embed origin events and maintenance/stat/rate-limit RPC access.
- `hosted-staging-mfa-smoke.ts`: synthetic staging TOTP flow with same-client
  protected PostgREST AAL1 denial, AAL2 success and exact cleanup. The revised
  form passed against retained isolated staging.
- `supabase-linked-command.ts`: fixed-recipe, shell-free wrapper. Production and
  unknown links are blocked; staging list/dry-run require the linked ref,
  `HOSTED_STAGING_PROJECT_REF`, `SUPABASE_URL`, `VITE_SUPABASE_URL`,
  `VITE_SUPABASE_PROJECT_ID` and explicit `--confirm-project-ref` to agree. The
  child environment is allowlisted; one-time repair and mutating push recipes
  are retired.
