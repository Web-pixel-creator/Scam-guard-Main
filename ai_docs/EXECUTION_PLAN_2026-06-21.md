# Ishonch Guard — Единый план работ (2026-06-21)

> Консолидация всего фидбэка этой сессии в один источник правды.
> Заменяет разрозненные списки. Порядок согласован: **сначала доверие к
> ответам бота → потом удобство модерации → потом вау-слой и рост.**

---

## 0. Главный принцип фазы

Не добавлять 20 новых фич. Самые болезненные места сейчас — **не «мало
функций», а доверие к ответам**. Пользователь теряет доверие мгновенно, когда
бот уверенно отвечает не по ситуации. Закрываем три вещи:

1. **Бот не должен путать контекст / выдумывать причины** (кейс голосового про
   доставку, где появились причины про «ставки/прогнозы»).
2. **Бот должен отвечать на уточнения как помощник**, а не запускать новую
   проверку (кейс «это меню сделано ИИ?»).
3. **QR/картинки честно разделять по типам**, не выдумывать домен/риск.

Архитектура rules-first неприкосновенна: AI только объясняет, не меняет вердикт.
Приватность (mask/redact/hash, репутация только после модерации) — без
компромиссов. В серую зону (MTProto, скрытые SCAM-метки, возраст аккаунта) **не
идём**.

---

## 1. Что уже внедрено (база)

- **Telegram-бот:** проверка текста, ссылок, номеров, username, QR/скринов,
  голосовых; SOS-сценарии (SMS-код, карта, перевод, APK, Telegram, AI-голос,
  работа, доставка, крипто/TON, грант, шантаж, несовершеннолетний, звонок);
  `/call`, Family Shield, Guardian Angel, Voice-in/STT, Voice-out/TTS,
  Risk Passport, `/report`.
- **Модерация:** приватный Telegram-чат — только маскированные уведомления;
  полные данные только в админке; повторные жалобы учитываются без дублей.
- **Дашборд:** вход админа, входящие жалобы, сущности/репутация, апелляции,
  подтверждение/отклонение.
- **Сайт:** публичная проверка, embed-виджет, Railway-деплой, official-numbers,
  scam-trends, honest impact counters.
- **Безопасность:** маскирование целей, service-role не уходит в браузер,
  лимиты на голос/TTS, CSP частично усилен, нет выдумывания скрытых
  Telegram-меток/возраста.

---

## 2. Сделано в ЭТОЙ сессии (working tree, ещё НЕ закоммичено)

> На ветке detached `HEAD` — перед коммитом создать ветку.
> Все тесты зелёные: **1630 passed / 86 files**, `tsc --noEmit` чисто,
> `qa:telegram-report` регенерирован.

- **Voice-out fallback** — убрано пугающее «не удалось отправить голосовой
  ответ»; единый спокойный тон: «Голос сейчас недоступен. Это не страшно —
  главное уже в тексте выше. Спокойно выполните первый шаг.» То же для лимита.
  → закрывает «мягкий UX лимита голоса» из P4.
- **SOS copy (частично P5):**
  - «Потерял Telegram» детальный: исправлен логический разрыв «Восстановите
    через SMS → Не получается» → теперь 1) завершить чужие сеансы, 2) облачный
    пароль, 3) *если не получается* — SIM, 4) предупредить, 5) заявление.
  - Заголовок компактной карточки «ВЕРНИТЕ ДОСТУП…» → «ЗАКРОЙТЕ ЧУЖОЙ ВХОД В
    TELEGRAM».
  - Кнопки: «Вернуть Telegram» → «Вернуть мой аккаунт»; «Пауза и проверка» →
    «Проверить с близким» (теперь честно описывает экран).

**Осталось по P5:** различить тексты для несовершеннолетних / шантажа / угрозы
публикацией (сейчас копипаста одного шаблона).

---

## 3. Зафиксированные решения (locked)

- **Voice-out = вариант C+** (не B). Короткий безопасный скрипт + предзаписанное
  аудио для частых SOS + кэш + cooldown + скрывать кнопку при исчерпании лимита.
  Рост лимитов как лекарство — **отклонено** (лечит симптом, не архитектуру).
- **Никакого MTProto / скрытых меток / возраста аккаунта.** Лучше честно, чем
  «магично» и неверно.
- **Лимиты на голос/TTS остаются** (защита от спама и сжигания токенов), но UX
  лимита — мягкий.
- **Rules-first**: AI никогда не решает вердикт.

---

## 4. Единый порядок выполнения

Для каждого пункта: **Зачем · Где в коде · Объём · Риск · Готово когда (DoD).**

### P1 — Voice False-Positive Fix 🐞 (доверие #1)
- **Зачем:** голосовое «оплата доставки только по карте» получило `🔴 Высокий
  риск` с причинами «закрытый канал со ставками/прогнозами», которых в тексте
  НЕТ. Выглядит как галлюцинация, хотя движок rules-first. Это #1 удар по
  доверию.
- **Где:** `src/lib/risk/rules.ts` (`gambling_prediction_promo`,
  `fake_delivery_payment`), мост голос→rules-input в
  `src/lib/telegram/handlers/check.ts` (handleVoice → runCheck), при участии
  image/voice evidence.
- **Объём:** расследовать, почему `gambling_prediction_promo` сматчился; сузить
  правило; правило «причина появляется только если паттерн реально в тексте»;
  добавить регрессионную фикстуру из этого транскрипта. «Доставка + карта» без
  ссылки/кода/APK/давления ≠ автоматический high-risk. Если спорно — писать
  «нужен контекст: кто прислал, куда ведёт, просят ли оплату/код/карту».
- **Риск:** средний (трогаем scoring; обязательны property+fixture тесты).
- **DoD:** транскрипт даёт корректный уровень и причины; новый тест ловит
  регресс; ни одно правило не приписывает несуществующий паттерн.

### P2 — Conversational Follow-up Router (доверие #2)
- **Зачем:** «Точно? / Почему? / Это ИИ? / Что это значит? / Что мне делать?»
  должны отвечаться по **последней проверке**, а не уходить в новый risk-check
  с карточкой «Недостаточно данных».
- **Где:** `src/lib/telegram/check-followup.ts` (расширить
  `classifyLastCheckFollowUp`), `src/lib/meta-intent.ts`,
  `session.server.ts` (снимок `lastCheck` уже есть). Ответ строится по
  контексту и проходит через `ai-output-safety.ts` firewall; **вердикт не
  меняется**.
- **Объём:** добавить интенты «это AI-макет?», «почему именно эта причина?»,
  свободные уточнения; короткий человеческий ответ по последнему контексту
  (пример: «Да, визуально похоже на AI-макет, но это не доказывает скам.
  Главный риск — какой адрес откроет QR и попросят ли оплату/код/карту»).
- **Риск:** средний (новый разговорный путь; firewall обязателен).
- **DoD:** перечисленные вопросы дают контекстный ответ без нового risk-result;
  firewall-тест на запрет выдачи секретов.

### P3 — QR / Image Precision Pass (доверие #3)
- **Зачем:** местами «подозрительный домен/осторожность» там, где видно
  ресторанное меню. Нельзя утверждать домен, если QR не прочитан.
- **Где:** `src/lib/risk/image-intelligence.ts`, `src/lib/risk/qr-decoder.ts`.
- **Объём:** честная таксономия — меню/информационный · Telegram-login ·
  банковский/2FA · payment · wallet/connect · invite/бот/капча/розыгрыш ·
  QR не прочитан · виден только макет. Правило: **QR не прочитан → не утверждать
  домен/риск**. Telegram-login QR от другого человека = высокий риск; меню =
  спокойный ответ.
- **Риск:** средний.
- **DoD:** фикстуры на каждый тип дают правильный класс и тон; «не прочитан» не
  выдумывает домен.

### P4 — Voice-out C+ (предзапись + кэш + cooldown + скрытие кнопки)
- **Зачем:** кнопка «работает один раз, потом падает» из-за лимита/провайдера.
- **Где:** `src/lib/telegram/voice-out.server.ts`,
  `src/lib/telegram/handlers/misc.ts`, `src/lib/telegram/emergency.ts`
  (`buildPanicVoiceOutText`).
- **Объём:** «Озвучить главный шаг» читает **только короткий безопасный скрипт**
  (без номеров/кодов/ссылок/seed). Для частых SOS — **предзаписанное аудио**
  (статичные .ogg по panicId+lang) → мгновенно, без лимитов и расходов API.
  TTS API — только для редких динамических подсказок. Кэш + cooldown от
  повторных кликов. При исчерпании лимита — **скрывать кнопку** или мягкий
  текст. Мягкий UX лимита — ✅ уже сделан в этой сессии.
- **Риск:** низкий-средний.
- **DoD:** частые SOS звучат из предзаписи мгновенно; 3 быстрых клика = 1
  генерация; при лимите кнопки нет либо мягкое сообщение.

### P5 — SOS Copy Polish (доделать)
- **Зачем:** убрать копипасту; разные ситуации — разные тексты.
- **Где:** `src/lib/telegram/emergency.ts` (сценарии 7 шантаж, 9 публикация,
  10 несовершеннолетний).
- **Объём:** различить тексты «Готовая фраза»/«Куда обратиться» для
  несовершеннолетних (мягче, без банковской лексики), шантажа, угрозы
  публикацией. ✅ «Потерял Telegram», кнопки, логический разрыв — уже сделано.
- **Риск:** низкий. **DoD:** тексты этих сценариев не идентичны; тон для <18 мягче.

### P6 — Dashboard Operator UX v1
- **Зачем:** дашборд работает, но выглядит как техпанель, не инструмент модератора.
- **Где:** `src/routes/admin.tsx`, `src/lib/admin.functions.ts`.
- **Объём:** «✓ СКАМ» → юридически мягкое «Подтвердить риск / В публичную базу».
  Метрики разделить: новые жалобы · подтверждено публично · цели на проверке ·
  всего проверок · апелляции (убрать путаницу «Сущностей: 0»). Карточка жалобы
  человеческая: что просили · почему риск · сколько жалоб · что решить.
  Detail-view для жалобы/цели. «Score: 5, AI: нет» → «Что видно / Что недоступно
  / Почему осторожно / Решение модератора». В UI явно: Telegram-чат — только
  уведомления, расследование в админке.
- **Риск:** низкий (UI). **DoD:** модератор понимает карточку без техжаргона.

### P7 — Report Flow Polish
- **Где:** `src/lib/report.functions.ts`, копирайт в
  `src/lib/telegram/bot-i18n.ts`, админка.
- **Объём:** тёплый финал «Вы помогли предупредить других. Мы проверим вручную.»
  Повтор: «На эту цель уже жаловались сегодня. Дубль не создаём, но учитываем
  сигнал.» В админке дубли = сила сигнала: «2 жалобы сегодня, последняя: …».
- **Риск:** низкий. **DoD:** нет dev-фраз; повтор показан как сигнал.

### P8 — Risk Passport v2 (username/phone, честно)
- **Где:** `src/lib/telegram/public-metadata.server.ts`,
  `src/lib/risk/phone-intelligence.ts`, `src/lib/risk/image-intelligence.ts`.
- **Можно показывать:** страна/оператор, не местный номер, совпадение с офиц.
  справочником, жалобы в нашей базе, странный/гиббериш username, похожесть на
  бренд, имперсонация бренда, публичный канал/пост; по скрину профиля — нет
  аватарки, странное имя, описание, ссылки (с пометкой уверенности).
- **Нельзя утверждать:** возраст аккаунта, скрытую SCAM-метку, жалобы Telegram,
  кому писал.
- **Риск:** низкий-средний. **DoD:** паспорт честен про «что видно / недоступно».

### P9 — Speed & Cost
- **Объём:** сразу «Проверяю…» / «Распознаю голос, до 10 сек»; кэш повторных
  проверок; не дёргать AI там, где rules достаточно; кэш TTS; позже
  Redis/Upstash rate-limit.
- **Где:** `src/lib/telegram/handlers/check.ts`, `runCheck` путь.
- **Риск:** средний (кэш-инвалидизация). **DoD:** воспринимаемая задержка ниже;
  низкосигнальные проверки не зовут AI.

### P10 — Security Sprint
- **Объём:** проверить, что `unsafe-inline` в CSP основного сайта реально убран
  (по OPEN_TASKS — уже nonce-based, нужно подтвердить); отдельно
  задокументировать CSP embed-виджета; **adversarial-тесты** (попытки заставить
  бота просить карту/код/seed/пароль — property в `rules.property.test.ts`);
  внешние сигналы URL: **Google Safe Browsing → URLhaus → PhishTank** как
  аддитивные reason-коды.
- **Где:** `src/server.ts` (CSP), `src/lib/risk/rules.ts` (новый URL-сигнал).
- **Риск:** низкий-средний. **DoD:** adversarial-набор зелёный; Safe Browsing
  даёт reason-код «ссылка в базе фишинга».

---

## 5. Продуктовый бэклог (после стабилизации)

Phone Reputation v1/v2 · публичная страница офиц. номеров (есть — расширить) ·
Public scam digest «схемы недели» · Scam call trainer (вирусный) · Embed partner
registry для банков/СМИ · Карта/индекс скама Узбекистана · публичные истории без
личных данных.

---

## 6. Анти-цели (что НЕ делаем)

MTProto и скрытые Telegram-метки · возраст аккаунта · публичные обвинения как
`@idoras` · рост лимитов голоса как «решение» · любые непроверённые заявления о
владельце номера/аккаунта.

---

## 7. Рекомендованный первый шаг

**P1 — Voice False-Positive Fix.** Это одновременно баг и самый видимый удар по
доверию. Старт: расследовать, почему `gambling_prediction_promo` сматчился на
голосовое про доставку, сузить правило и зафиксировать регрессионным тестом.
Дальше по порядку P2 → P3 → P4 → P5 (доделать) → P6 → P7 → P8 → P9 → P10.
